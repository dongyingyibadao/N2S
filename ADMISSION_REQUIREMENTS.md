# N2S 案例准入要求

每个新迁移项必须提供以下信息；缺失字段应明确写为 `pending` 或 `not_available`，不能省略。

## 证据层级

每个案例必须声明当前最高证据层级。较高层级包含但不替代较低层级的原始 A/B 记录。

- `module`：必需。使用固定合成输入或可归档的中间张量，对修改前后的局部算子/模块做功能、数值、
  性能和显存 A/B。新案例达到这一层即可按 `exploratory` 保存和贡献。
- `integration`：按范围要求。用随机初始化、小配置或录制的中间输入运行实际最小调用链，证明匹配的
  helper、cache、compile 分支或 adapter 确实被执行；不要求下载公开模型权重或数据集。
- `model`：条件要求。只有结论涉及 action/logit、端到端推理、训练/梯度、checkpoint/release、
  预处理语义、模型级性能，或高风险路径无法由 integration 证明时，才要求对应模型和 checkpoint。

批准范围以已完成证据层级为上限。仅有 `module` 证据的结论必须限定设备、框架版本、算子、dtype、
shape/range 和模式；不得外推为模型级正确性、模型级性能或所有 NPU/CUDA 的通用结论。

## 可复现输入

- 修改前和修改后的完整代码或固定 revision，以及可重建 diff。
- 框架、驱动、工具包、Python 和关键依赖版本。
- 物理设备型号和数量；虚拟化、容器、可见设备设置也应记录。
- 使用 checkpoint 时记录来源、revision、文件大小和 SHA256；模块/integration 案例未使用时明确写
  `not_applicable`，不为满足格式而下载权重。
- 输入数据或可生成输入的固定描述、形状、dtype、预处理参数。
- 所有随机源的 seed 和确定性设置。

## 结果证据

- 最大绝对误差、相对误差、RMSE、余弦相似度等原始数值，不只保存 pass/fail。
- 功能结果、异常类型和完整异常消息。
- 申请 `model` 层级时，记录固定 checkpoint/输入/seed 下的 action delta、top-k 一致率、loss 和
  梯度有限性；不涉及的模型输出明确写 `not_applicable`。
- 同一物理设备、同一环境、同一负载上的修改前后性能 A/B。
- 每次计时的原始样本、预热次数、median、p95 和峰值显存。
- profiler、dump、自动迁移等工具引入的时间、显存和行为副作用。

不设置跨模型、跨设备的统一数值或性能阈值。跨硬件绝对速度只能作为背景信息，不能作为
迁移质量、代码优劣或性能收益结论。

## 范围与风险

- 声明适用层级：算子、框架版本、模型、checkpoint/release 或设备。
- 记录已知缺点、行为变化、monkey-patch 或全局状态副作用。
- 给出可操作的回退方法。
- 列出未覆盖的模型、dtype、shape、训练/推理模式、设备和框架版本。
- 区分设备迁移必要修改与模型本身、release 演进、checkpoint 兼容修改。

## 审批与自动应用

所有新条目默认 `promotion_status: exploratory`。证据评定可由维护者或明确标识的受托 agent 记录，
至少包含审查主体、日期、证据 revision、证据层级、批准范围和限制条件。自动化测试通过不能自行
扩大适用范围；启用 `auto_apply` 前仍须由仓库维护者确认审批记录和回退策略。

## 通用规则晋级

通用规则不能只保存一段 patch。每个规则包必须同时记录：修改前后的代码形态、修改原因、修改
方法、功能与数值影响、性能与显存影响、适用前置条件、反例、已知副作用、验证命令和回退方法。
detector、codemod、validator 与 fixtures 必须和文档中的适用范围一致。

数值或运行时规则首先需要可独立运行的 `module` A/B：固定输入和 seed、CPU 高精度或原 CUDA
参考，以及同一 NPU 上修改前后的原始样本、median、p95 和峰值显存。每个获批 adapter 至少需要
`integration` 证据，或从批准范围中明确排除。仅有模块证据也可以形成 candidate，或批准为范围
严格限定的模块规则。

只有申请跨项目/模型家族的广泛结论时，才要求源案例加两个独立项目和至少两个模型家族；只有
申请模型输出、训练、checkpoint/release 或模型级性能结论时，才要求固定 checkpoint 的模型级
对照。框架 API 的跨项目结论仍至少覆盖两个项目和两个相关框架版本。checkpoint、预处理和模型
语义变化默认保持窄范围兼容规则，不得包装成设备通用规则。

阈值必须写在每条规则自己的验收契约中，不设置全库统一阈值。只有 `approved`、证据层级覆盖
声明范围且审批范围覆盖目标环境的规则可以自动应用；其余状态必须 fail closed。
