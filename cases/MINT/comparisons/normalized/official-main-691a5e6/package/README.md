# LeRobot MINT policy

This package registers the full MINT-4B policy as `--policy.type=mint`.

```bash
pip install -e ./policy/lerobot_policy_mint
```

See the repository README for tokenizer, training, and evaluation commands.
